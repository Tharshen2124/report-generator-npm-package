/* export default function getCenterAlignCenterPoint(value: number, nextValue: number, width: number): number {
    const widthAfterMinus = (value + nextValue) - width
    const margin = (nextValue - value) / 2
    const point = value + margin

    return point
} */