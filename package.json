{
  "name": "report-generator-npm-package",
  "version": "1.0.0",
  "description": "Generates a report based on the test cases and their results",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "script": "npx tsc && node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pdfkit": "^0.14.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.5",
    "@types/pdfkit": "^0.13.4",
    "typescript": "^5.4.4"
  }
}
